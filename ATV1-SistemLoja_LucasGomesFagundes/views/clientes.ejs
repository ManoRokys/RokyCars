<%- include ('partials/header.ejs') %>
<%- include ('partials/navbar.ejs') %>

<div class="container">
    <div class="card">
        <div class="card-header">
            <h1>Jogadores</h1>
        </div>
        <div class="card-body">
            <table>
                <thead>
                    <tr>
                        <th></th>
                        <th>Nome:</th>
                        <th>ID:</th>
                        <th>País:</th>
                    </tr>
                </thead>
                <tbody>
                    <% clientes.forEach(cliente => { %>
                        <tr>
                            <td><img src="imgs/<%= cliente.img %>.jpg" class="profile-img" alt="<%= cliente.nome %>"></td>
                            <td><%= cliente.nome %></td>
                            <td><%= cliente.ID %></td>
                            <td><%= cliente.country %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</div>

<%- include ('partials/footer.ejs') %>